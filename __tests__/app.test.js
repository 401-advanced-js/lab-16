'use strict';

describe('POL', () => {
  it('should be alive!', () => {
    expect(true).toBeTruthy();
  });
});

describe('Should be able to read a file', () => {
  xit('Can read a file', () => {
    expect();
    xit('Can upper string the file contents correctly', () => {
      expect();
      xit('Can write to a file with those file contents', () => {
        
      });
    });
  });
  xit('Returns an error if the file does not exist', () => {
    expect();
  });
});
